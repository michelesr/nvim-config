# Neovim configuration

My [neovim](https://neovim.io/) configuration.

## Installation

Just run neovim. Use `:PackUpdate` to update plugins, `:PackSync` to install from lockfile and `:Mason` to manage LSPs.
